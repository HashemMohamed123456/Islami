import 'package:flutter/material.dart';
class SebhaWidget extends StatelessWidget {
  const SebhaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sebha Widget'),
    );
  }
}
